# Copy to .env (gitignored):  cp env.example .env
# Used everywhere: mobile (bundled into the app), desktop release (--dart-define-from-file).

REOWN_PROJECT_ID=your_reown_project_id_from_cloud.reown.com
GATEWAY_URL=https://gateway.erebrus.io
EREBRUS_WEB_ORIGIN=https://erebrus.io